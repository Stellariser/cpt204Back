<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.First.dao.UserMapper">

    <resultMap id="UserResultMap" type="com.First.pojo.User">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="secret_question" jdbcType="BIGINT" property="secret_question" />
        <result column="secret_answer" jdbcType="VARCHAR" property="secret_answer" />
        <result column="avator" jdbcType="VARCHAR" property="avator" />
        <result column="gender" jdbcType="VARCHAR" property="gender" />
        <result column="grade" jdbcType="VARCHAR" property="grade" />
        <result column="major" jdbcType="VARCHAR" property="major"/>
        <result column="personal_info" jdbcType="VARCHAR" property="personal_info"/>
    </resultMap>

    <select id="listUser" resultType="com.First.pojo.User">
        select * from t_user

    </select>

    <select id="queryUserByName" resultMap="UserResultMap">
        select * from t_user where username=#{username}
    </select>

    <select id="queryUserById" resultMap="UserResultMap">
        select * from t_user where id=#{id}
    </select>

    <insert id="addUser" parameterType="com.First.pojo.User">
        insert into t_user(username,password,secret_question,secret_answer,avator) values(#{username},#{password},#{secret_question},#{secret_answer},#{avator})
    </insert>

    <update id="updatePersonalInfo" parameterType="com.First.pojo.User">
        update t_user set username=#{username},gender=#{gender},grade=#{grade},personal_info=#{personal_info} where id =#{id}
    </update>

</mapper>
